@page "/productadd"
@using MudBlazor
@rendermode InteractiveAuto
@inject ISnackbar Snackbar

<h3>Add Product</h3>


<EditForm Model="product" OnValidSubmit="OnSubmit" Enhance FormName="ProductAdd">
    <DataAnnotationsValidator />
    <ValidationSummary />
    <div class="row mb-3">
        <label for="Product" class="col-form-label col-md-3">Product: </label>
        <div class="col-md-8">
            <InputText id="Product" class="form-control col-md-8" @bind-Value="@product.Name" placeholder="Enter Product name"></InputText>
            <ValidationMessage For="@(() => product.Name)" />
        </div>
    </div>

    <div class="row mb-3">
        <label for="Price" class="col-form-label col-md-3">Price: </label>
        <div class="col-md-8">
            <InputNumber id="Price" class="form-control col-md-8" @bind-Value="@product.Price" placeholder="Enter Price"></InputNumber>
            <ValidationMessage For="@(() => product.Price)" />
        </div>
    </div>

    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Go to Products
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <button type="submit" class="btn btn-outline-primary ms-1">Submit</button>
        </div>
    </nav>

</EditForm>
